
creating a project

//!
1. npm init
// add dependencies and npm install or
2. npm install —save mongoose


//! then create index script

const express = require('express');

// create instance of express (let) - local = un block lifespance
let app  = express();


// looks for the port number
app.listen('8080', function() {
  console.log("hello everybody!");
});


//! node deamon - starts server and starts automatically

npm install --save nodemon
// if want to install global
npm install -g nodemon


// npm install --save ejs